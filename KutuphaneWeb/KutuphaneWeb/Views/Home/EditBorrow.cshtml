@model KutuphaneWeb.Models.Borrow

@{
    ViewBag.Title = "EditBorrow";
}

<h2>Emanet Düzenle</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Emanet</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.BorrowDate, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.BorrowDate)
                @Html.ValidationMessageFor(model => model.BorrowDate)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.EstimatedReturnDate, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EstimatedReturnDate)
                @Html.ValidationMessageFor(model => model.EstimatedReturnDate)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ReturnDate, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ReturnDate)
                @Html.ValidationMessageFor(model => model.ReturnDate)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.BookId, "Book", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select name="WriterId">
                    @foreach (KutuphaneWeb.Models.Book bk in ViewBag.Books)
                    {
                        if (bk.Id == Model.BookId)
                        {
                            <option selected value="@bk.Id">@bk.Name</option>
                        }
                        else
                        {
                            <option value="@bk.Id">@bk.Name</option>
                        }
                    }
                </select>
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.MemberId, "Member", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select name="TypeId">
                    @foreach (KutuphaneWeb.Models.Member mb in ViewBag.Members)
                    {
                        if (mb.Id == Model.MemberId)
                        {
                            <option selected value="@mb.Id">@mb.NameSurname</option>
                        }
                        else
                        {
                            <option value="@mb.Id">@mb.NameSurname</option>
                        }
                    }
                </select>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Emanet Güncelle" class="btn btn-default" />
            </div>
        </div>

        
    </div>

    <div>
        @Html.ActionLink("Emanetlere Dön", "Borrows")
    </div>
}

@section scripts{
    <script>
        $(function () {
            $("input,select").addClass("form-control");
        });
    </script>

}